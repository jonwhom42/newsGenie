# NewsGenie dependencies
# Tested with Python 3.10–3.12

streamlit>=1.34.0
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0
requests>=2.31.0

# Web search (DuckDuckGo). The package was renamed from
# `duckduckgo-search` to `ddgs`; we install the new name and the
# code falls back to the old import automatically.
ddgs>=6.0.0

python-dotenv>=1.0.0
